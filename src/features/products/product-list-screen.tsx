import {Text, View} from 'react-native';

export const ProductListScreen = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
      }}>
      <Text style={{fontSize: 30, color: 'black'}}>Product List Screen</Text>
    </View>
  );
};
